// This file is needed for Next.js pages directory structure
// It's a minimal version to satisfy the build requirements
export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
